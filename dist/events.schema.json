{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/bigsapper/drag-events-aggregator/blob/main/dist/events.schema.json",
  "title": "Drag Racing Events",
  "description": "Array of structured drag racing event records extracted from track websites and event flyers.",
  "type": "array",
  "items": {
    "$ref": "#/definitions/Event"
  },
  "definitions": {
    "Event": {
      "type": "object",
      "required": ["id", "title", "event_type", "track", "dates", "confidence", "flyers", "created_at", "updated_at"],
      "properties": {
        "id": {
          "type": "string",
          "format": "uuid",
          "description": "Unique identifier for the event (UUID v4)."
        },
        "title": {
          "type": "string",
          "description": "Full event name as shown on the flyer or listing."
        },
        "event_type": {
          "type": "string",
          "enum": ["bracket", "points_race", "test_n_tune", "no_prep", "grudge", "specialty", "test_day", "unknown"],
          "description": "Primary type of drag racing event."
        },
        "series": {
          "type": ["string", "null"],
          "description": "Sanctioning body or series name (e.g. NHRA, IHRA, or a local bracket series). Null if not specified."
        },
        "track": {
          "$ref": "#/definitions/Track"
        },
        "dates": {
          "$ref": "#/definitions/DateRange"
        },
        "times": {
          "$ref": "#/definitions/EventTimes"
        },
        "classes": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Racing classes or categories listed on the flyer (e.g. 'Super Pro', 'Pro', '5.90 Index')."
        },
        "fees": {
          "$ref": "#/definitions/Fees"
        },
        "contact": {
          "$ref": "#/definitions/Contact"
        },
        "confidence": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Extraction confidence score (0.0–1.0). Reflects how clearly the source material presented the data. Values below 0.5 should be treated with caution."
        },
        "unclear_fields": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Field names where the extracted value was uncertain. Consumers may wish to flag or exclude these fields."
        },
        "notes": {
          "type": ["string", "null"],
          "description": "Additional information from the flyer not captured by structured fields."
        },
        "flyers": {
          "type": "array",
          "items": { "$ref": "#/definitions/FlyerRef" },
          "description": "Source flyer images or listings that contributed to this event record. Multiple entries indicate the event appeared across more than one source."
        },
        "created_at": {
          "type": "string",
          "format": "date-time",
          "description": "ISO 8601 timestamp (UTC) when this event record was first created."
        },
        "updated_at": {
          "type": "string",
          "format": "date-time",
          "description": "ISO 8601 timestamp (UTC) when this event record was last modified."
        }
      },
      "additionalProperties": false
    },
    "Track": {
      "type": "object",
      "required": ["name"],
      "properties": {
        "name": {
          "type": "string",
          "description": "Official track name."
        },
        "city": {
          "type": ["string", "null"],
          "description": "City where the track is located. Null if not determinable from the source."
        },
        "state": {
          "type": ["string", "null"],
          "description": "Two-letter US state abbreviation (e.g. 'TX'). Null if not determinable."
        }
      },
      "additionalProperties": false
    },
    "DateRange": {
      "type": "object",
      "required": ["start"],
      "properties": {
        "start": {
          "type": "string",
          "format": "date",
          "description": "Event start date in YYYY-MM-DD format."
        },
        "end": {
          "type": ["string", "null"],
          "format": "date",
          "description": "Event end date in YYYY-MM-DD format. Null for single-day events."
        }
      },
      "additionalProperties": false
    },
    "EventTimes": {
      "type": "object",
      "properties": {
        "gates_open": {
          "type": ["string", "null"],
          "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$",
          "description": "Time gates open in 24-hour HH:MM format. Null if not specified."
        },
        "registration_opens": {
          "type": ["string", "null"],
          "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$",
          "description": "Registration open time in 24-hour HH:MM format. Null if not specified."
        },
        "race_start": {
          "type": ["string", "null"],
          "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$",
          "description": "First round start time in 24-hour HH:MM format. Null if not specified."
        }
      },
      "additionalProperties": false
    },
    "Fees": {
      "type": "object",
      "properties": {
        "entry": {
          "type": ["string", "null"],
          "description": "Entry fee as printed on the flyer (e.g. '$60/class', '$100 per car'). Raw string — not normalized."
        },
        "spectator": {
          "type": ["string", "null"],
          "description": "Spectator admission fee as printed on the flyer. Raw string — not normalized."
        }
      },
      "additionalProperties": false
    },
    "Contact": {
      "type": "object",
      "properties": {
        "phone": {
          "type": ["string", "null"],
          "description": "Contact phone number as printed on the flyer."
        },
        "email": {
          "type": ["string", "null"],
          "format": "email",
          "description": "Contact email address."
        },
        "website": {
          "type": ["string", "null"],
          "format": "uri",
          "description": "Event or track website URL."
        }
      },
      "additionalProperties": false
    },
    "FlyerRef": {
      "type": "object",
      "required": ["file", "processed_at"],
      "properties": {
        "file": {
          "type": "string",
          "description": "Filename of the source flyer image, or source URL for text-based listings."
        },
        "phash": {
          "type": ["string", "null"],
          "description": "Perceptual hash (pHash) of the flyer image used for duplicate detection. Null for text-based listings."
        },
        "processed_at": {
          "type": "string",
          "format": "date-time",
          "description": "ISO 8601 timestamp (UTC) when this flyer was processed."
        }
      },
      "additionalProperties": false
    }
  }
}
